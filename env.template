# Copy this file to .env and fill in your values
# cp env.template .env

# Server Configuration
PORT=3060
NODE_ENV=development

# HGT Data Path
HGT_DIR=./data/hgt

# PostgreSQL Database Configuration
# Make sure PostgreSQL is installed and running
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hexagonal_tiles
DB_USER=postgres
DB_PASSWORD=

# Notes:
# - Leave DB_PASSWORD empty if using local PostgreSQL without password
# - Create the database first: psql -U postgres -c "CREATE DATABASE hexagonal_tiles;"
# - Tables will be created automatically on first server start

